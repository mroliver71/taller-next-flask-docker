apiVersion: v1
kind: Service

metadata:
  name: mi-app-web-service

spec:
  type: NodePort

  selector:
    app: mi-app-web

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080